{
  "name": "gqlens",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "publish": "turbo run publish",
    "lint": "oxlint -c oxlint.config.mts --deny-warnings --report-unused-disable-directives packages",
    "format": "oxfmt --check .",
    "format:write": "oxfmt --write .",
    "verify": "pnpm run lint && pnpm run format && turbo run typecheck build test"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "oxfmt": "^0.53.0",
    "oxlint": "^1.68.0",
    "tsdown": "^0.22.2",
    "turbo": "^2.9.16",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=24.12"
  },
  "packageManager": "pnpm@11.5.2"
}
